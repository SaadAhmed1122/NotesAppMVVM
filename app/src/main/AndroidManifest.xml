<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mvvm.notesappmvvm">

    <application
        android:allowBackup="true"
        android:icon="@drawable/noteslogo"
        android:label="@string/app_name"

        android:roundIcon="@drawable/noteslogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotesAppMVVM">
        <activity
            android:name=".Activities.UpdateNoteAct"
            android:exported="true" />
        <activity
            android:name=".Activities.InsertNoteAct"
            android:exported="true" />
        <activity
            android:name=".Activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>